# 香港招聘数据爬取与分析

**Date**: June 1, 2026 at 12:31 PM
**Turns**: 3
**Source**: [Gemini Chat](https://gemini.google.com/app/8869d94ad5678455)

---

## Turn 1

### 👤 User

如何爬取香港招聘网站的岗位jd数据作技术需求分析

### 🤖 Assistant

爬取香港招聘网站的岗位 JD（Job Description）数据并进行技术需求分析，是一个非常典型的数据采集 + 文本挖掘（NLP）项目。香港常用的主流招聘网站包括 **JobsDB、LinkedIn、Indeed、HKGoodJobs** 等。
下面为你梳理一套完整的全栈实现方案，从数据爬取到分析展现：

## 🚀 第一阶段：数据爬取（Data Crawling）
针对香港网站，推荐优先选择 **JobsDB**（香港最大的招聘平台，属于 SEEK 集团）或 **Indeed**。

### 1. 技术选型

- **静态/API 爬取（首选）：**`Requests` + `BeautifulSoup`。很多现代招聘网站（如 JobsDB 的新版）采用前后端分离，可以通过浏览器 F12 抓包直接找到其后端 API，返回的是 JSON 数据，解析效率最高。
- **动态渲染（备选）：**`Selenium` 或 `Playwright`。如果网站有严格的反爬（如 LinkedIn）或强 JavaScript 渲染，可以用自动化工具模拟真实浏览器行为。

### 2. 香港网站反爬注意事项

- **境外 IP 问题：** 部分香港本地网站对特定区域 IP 有限制或高频拦截，建议使用香港或海外节点的代理 IP（Proxy）。
- **请求头（Headers）：** 必须伪造完整的 `User-Agent`，并加上 `Referer`。
- **频率控制：** 设置随机延迟 `time.sleep(random.randint(2, 5))`，避免触发 Cloudflare 等防爬墙。

## 📊 第二阶段：数据清洗与存储（Data Cleaning）
爬取下来的 JD 数据通常夹杂着大量的 HTML 标签、英文缩写和标点符号。

### 1. 字段设计
建议提取并存储以下字段：
`Job_ID`, `Title`（职位名称）, `Company`（公司）, `Location`（工作地点：如中环、观塘）, `Salary`（薪资，通常是港币 HKD）, `JD_Text`（详细描述文本）, `Url`（原始链接）。

### 2. 文本清洗

- 去除 HTML 标签、特殊字符。
- 统一大小写（例如：将 `Python`、`python`、`PYTHON` 统一为 `Python`）。

## 🧠 第三阶段：技术需求分析（Text Mining & NLP）
这是项目的核心。由于香港的岗位 JD **绝大多数是全英文，或者中英文夹杂（Code-switching）**，处理时需要兼顾英文和中文。

### 1. 方案 A：基于关键词词表（规则匹配 - 高效准确）
如果你已经有明确的技术目标（例如想分析前端、大数据或 AI 方向），直接用正表达式或词表匹配是最精准的。

- **步骤：** 建立一个技术栈字典（如 `['Python', 'SQL', 'React', 'AWS', 'Docker', 'Spark']`）。
- **实现：** 用 Python 的 `re` 模块或 `spaCy` 在清洗后的 JD 文本中查找这些关键词。

### 2. 方案 B：基于 NLP 命名实体识别（NER - 发现新词）
如果你想让系统自动识别出哪些是技术硬技能（Hard Skills），可以使用自然语言处理工具。

- **工具推荐：**`spaCy`（处理英文非常强大）或 `Stanza`。
- **操作：** 训练或使用现成的 IT 领域实体识别模型，将文本中的术语抽取出来。

### 3. 方案 C：使用大语言模型（LLM - 智能化、门槛低）
如果数据量不是千万级，推荐直接调用大模型 API（如 DeepSeek、GPT-4o）来进行结构化提取。

- **Prompt 示例：**"请阅读以下香港招聘岗位的 JD 文本，提取出其中要求的【编程语言】、【框架与工具】、【云服务与数据库】以及【软实力要求】。请直接以 JSON 格式输出，不要包含任何解释。"

## 📈 第四阶段：数据可视化（Visualization）
有了结构化数据后，可以从以下几个维度进行分析：

1. **技术热度榜（词云图/条形图）：** 统计哪些技术栈（如 React vs Vue，Python vs Java）在香港的招聘中出现频率最高。
2. **薪资与技术关联度（散点图/箱线图）：** 分析掌握哪些技术（如 AWS、GenAI）的岗位普遍薪资更高（注意将 HKD 薪资区间标准化）。
3. **行业/区域分布（热力图）：** 观察金融业（中环、金钟）与科技业（数码港、科学园）在技术需求上的差异。
*常用工具：Python 的 Pandas + Matplotlib / Seaborn，或者将数据导出到 PowerBI / Tableau 中展示。*

## ⚖️ 法律与合规提示（重要）
在爬取香港网站时，务必遵守以下原则：

- **阅读 robots.txt：** 查看目标网站允许爬取的路径。
- **非商业用途：** 确保分析数据仅用于个人研究、学术报告或求职参考，切勿将爬取到的商业数据进行转售。
- **不要造成 DDoS 效果：** 控制并发速度，不要把对方服务器爬挂。
你目前是想针对特定的行业（如金融科技 FinTech）进行分析，还是想做全行业的 IT 技术盘点？我可以为你提供一段基础的 Python 爬虫或文本解析的伪代码。

## Turn 2

### 👤 User

根据以上内容写一篇技术文档

### 🤖 Assistant

为了将上述爬取与分析的思路转化为研发团队或个人开发者可执行的落地方案，下面为您编写一份结构完整的《香港招聘网站数据爬取与技术需求分析系统架构及实现方案》技术文档。

# 技术文档：香港招聘网站数据爬取与技术需求分析系统

## 1. 概述 (Introduction)

### 1.1 目的
本系统旨在通过自动化手段采集香港主流招聘网站（以 JobsDB、Indeed 为主）的 IT 相关岗位招聘详情（Job Description, JD），利用自然语言处理（NLP）及大语言模型（LLM）技术对文本进行结构化清洗与挖掘，最终输出香港本地 IT 市场的技术栈热度、薪资分布及行业需求趋势，为企业人才选拔或个人求职提供数据决策支持。

### 1.2 目标网站分析

- **JobsDB (SEEK Group)**：香港最大的招聘平台，岗位量大，近年来网站升级为前後端分离架构（GraphQL / RESTful API），利于直接抓取 API。
- **Indeed HK**：数据量大，反爬虫策略相对温和，适合作为数据补充。
- **文本特征**：香港 IT 岗位 JD 呈现高度的中英夹杂（Code-switching）或全英文特征，专有名词（如：Java, AWS, CI/CD）占比高。

## 2. 系统总体架构 (System Architecture)
本系统整体分为四层：**数据采集层、数据清洗与存储层、算法与文本挖掘层、数据可视化层**。

```
+-------------------------------------------------------------+
|                     数据采集层 (Requests / Playwright)      |
+-------------------------------------------------------------+
                               |
                               v
+-------------------------------------------------------------+
|              数据清洗与存储层 (Pandas / MongoDB)             |
+-------------------------------------------------------------+
                               |
                               v
+-------------------------------------------------------------+
|             算法与文本挖掘层 (spaCy / DeepSeek API)         |
+-------------------------------------------------------------+
                               |
                               v
+-------------------------------------------------------------+
|                 数据可视化层 (Streamlit / PowerBI)          |
+-------------------------------------------------------------+

```

## 3. 详细设计与核心代码实现 (Detailed Design)

### 3.1 数据采集层 (Data Crawling Layer)
针对 JobsDB 的现代 API 架构，我们采用 `requests` 直接请求其后端的 GraphQL 或 JSON 接口。若遇到强反爬阻碍，则切换为 `playwright` 模拟浏览器行为。

#### 3.1.1 核心爬虫实现（以模拟请求 API 为例）

```python
import requests
import time
import random
import logging

logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')

class JobsDBCrawler:
    def __init__(self):
        self.base_url = "https://hk.jobsdb.com/api/jobs"  # 示例API路径，具体根据F12抓包调整
        self.headers = {
            "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
            "Referer": "https://hk.jobsdb.com/",
            "Accept": "application/json"
        }
        # 建议配置海外/香港本地代理 IP
        self.proxies = {
            "http": "http://your_hk_proxy_ip:port",
            "https://": "http://your_hk_proxy_ip:port"
        }

    def fetch_jobs(self, keyword, page=1):
        params = {
            "key": keyword,
            "location": "Hong Kong",
            "page": page
        }
        try:
            logging.info(f"正在爬取关键词 [{keyword}] 第 {page} 页...")
            response = requests.get(self.base_url, headers=self.headers, params=params, timeout=10)
            if response.status_code == 200:
                return response.json()
            else:
                logging.error(f"请求失败，状态码: {response.status_code}")
                return None
        except Exception as e:
            logging.error(f"请求发生异常: {e}")
            return None

    def run(self, keyword, max_pages=5):
        all_data = []
        for page in range(1, max_pages + 1):
            data = self.fetch_jobs(keyword, page)
            if data:
                all_data.append(data)
                # 随机延迟，防止被反爬墙阻断
                time.sleep(random.uniform(2.5, 5.0))
            else:
                break
        return all_data

```

### 3.2 数据清洗与结构化 (Data Cleaning & Storage)
将抓取到的原生 JSON/HTML 数据进行清洗，去除多余的 HTML 标签，并将岗位标准信息存入数据库。

#### 3.2.1 数据表/集合设计 (MongoDB / JSON Schema)

```json
{
  "job_id": "HK_10023948",
  "title": "Senior Full Stack Engineer (Node.js + React)",
  "company": "HK Fintech Limitied",
  "location": "Central",
  "salary_raw": "HK$45,000 - HK$60,000 /month",
  "salary_min": 45000,
  "salary_max": 60000,
  "jd_text": "We are looking for a software developer proficient in React and AWS cloud services..."
}

```

#### 3.2.2 文本清洗代码

```python
import re
from bs4 import BeautifulSoup

def clean_jd_text(raw_html):
    """清除HTML标签并规范化文本"""
    if not raw_html:
        return ""
    # 去除HTML标签
    text = BeautifulSoup(raw_html, "html.parser").get_text(separator=" ")
    # 去除多余的空格和换行
    text = re.sub(r'\s+', ' ', text)
    return text.strip()

```

### 3.3 技术需求提取层 (Text Mining Layer)
由于香港 JD 以英文为主，本方案设计了两种提取模式：**规则匹配模式**（性能高、覆盖已知词）与 **LLM 提取模式**（智能化、适应性强）。

#### 3.3.1 方案 A：基于关键词词表的正则匹配

```python
import re

# 预定义的 IT 技术栈词表
TECH_DICT = {
    "Languages": ["python", "java", "typescript", "javascript", "golang", "c\\+\\+", "php", "ruby"],
    "Frameworks": ["react", "vue", "angular", "django", "spring boot", "express", "next\\.js"],
    "Cloud_DevOps": ["aws", "azure", "gcp", "docker", "kubernetes", "cicd", "jenkins"],
    "Databases": ["mysql", "postgresql", "mongodb", "redis", "oracle", "dynamodb"]
}

def extract_skills_by_rules(text):
    text_lower = text.lower()
    extracted_skills = {}
    
    for category, keywords in TECH_DICT.items():
        extracted_skills[category] = []
        for kw in keywords:
            # 边界匹配，防止将 'java' 误匹配到 'javascript'
            pattern = r'\b' + kw + r'\b'
            if re.search(pattern, text_lower):
                # 规范化输出格式
                display_name = kw.replace('\\', '')
                extracted_skills[category].append(display_name.capitalize())
                
    return extracted_skills

```

#### 3.3.2 方案 B：基于大语言模型 (LLM) API 的高级提取
对于复杂的、隐含的架构能力（如：Microservices、Event-driven），使用 LLM 能够更好地结构化。

```python
import openai

def extract_skills_by_llm(jd_text):
    client = openai.OpenAI(
        api_key="your_deepseek_or_openai_api_key",
        base_url="https://api.deepseek.com/v1" # 示例以DeepSeek为例
    )
    
    prompt = f"""
    You are an expert IT technical recruiter in Hong Kong. 
    Analyze the following Job Description and extract technical requirements.
    Return ONLY a valid JSON object with keys: 'Programming_Languages', 'Frameworks_Libraries', 'Cloud_DevOps', 'Databases'.
    
    Job Description:
    \"\"\"{jd_text}\"\"\"
    """
    
    try:
        response = client.chat.completions.create(
            model="deepseek-chat",
            messages=[{"role": "user", "content": prompt}],
            response_format={"type": "json_object"},
            temperature=0.1
        )
        return response.choices[0].message.content
    except Exception as e:
        print(f"LLM 提取失败: {e}")
        return None

```

### 3.4 数据可视化层 (Data Visualization)
清洗与抽取完成后，使用 `pandas` 加载数据，通过 `matplotlib` 或 `streamlit` 快速呈现报表。

#### 3.4.1 香港 IT 技术栈热度分析展示

```python
import pandas as pd
import matplotlib.pyplot as plt

# 假设已将提取的技能扁平化存储在 CSV 中
# 字段样例：job_id, skill, category
def plot_top_skills(csv_path):
    df = pd.read_csv(csv_path)
    
    # 统计出现频率最高的前 15 个技术栈
    top_skills = df['skill'].value_counts().head(15)
    
    plt.figure(figsize=(10, 6))
    top_skills.plot(kind='barh', color='skyblue')
    plt.gca().invert_yaxis()
    plt.title('Top 15 In-Demand IT Skills in Hong Kong')
    plt.xlabel('Number of Job Postings')
    plt.ylabel('Skill / Technology')
    plt.tight_layout()
    plt.savefig('hk_tech_trends.png')
    plt.show()

```

## 4. 关键技术攻关与风险控制 (Risks & Mitigations)

| 风险分类 | 具体风险表现 | 应对策略与技术手段 |
| --- | --- | --- |
| 反爬虫阻断 | 频繁请求被封 IP，遇到 Cloudflare 5秒盾拦截。 | 1. 接入高品质香港本地住宅代理 IP 池。2. 采用 Playwright Stealth 插件隐藏自动化工具痕迹。3. 模拟人类浏览行为，加入滑动和不规则等待。 |
| 薪资标准不一 | 港币计算方式有 month (月薪) 或 annum (年薪)，且包含范围。 | 编写解析清洗函数，统一将年薪除以 12，并提取出 min_salary 和 max_salary 两个数字型字段。 |
| 混合语识别差 | 文本中出现 "懂 React, AWS 优先"，传统英文分词器失效。 | 提取技术实体时统一转为小写，采用词表边界正则匹配或大模型处理，不依赖复杂的中文分词。 |

## 5. 系统部署与运行指南 (Deployment)

### 5.1 环境依赖
创建 `requirements.txt` 文件：

```plaintext
requests==2.31.0
beautifulsoup4==4.12.3
playwright==1.42.0
pandas==2.2.1
openai==1.14.1
matplotlib==3.8.3

```

### 5.2 运行步骤

1. **安装依赖**：`pip install -r requirements.txt`
2. **初始化浏览器（如需自动化）**：`playwright install`
3. **配置密钥**：在代码或环境变量中配置您的代理 IP 及大模型 API Key。
4. **执行主程序**：运行爬虫脚本，数据落库后调用分析模块输出图表。

## Turn 3

### 👤 User

写一个项目readme

### 🤖 Assistant

这里为你编写了一份规范、专业的开源风格 `README.md` 项目说明文档。你可以根据实际的代码仓库结构进行微调。

# 🚀 HK-JobMarket-Analyzer: 香港招聘网站数据爬取与技术需求分析系统
本系统是一个专注于香港 IT 招聘市场的数据采集与文本挖掘项目。系统自动化爬取香港主流招聘网站（如 JobsDB、Indeed）的岗位数据，利用 **正则表达式** 与 **大语言模型 (LLM)** 对中英夹杂的 Job Description (JD) 进行智能结构化清洗，最终可视化输出香港本地 IT 市场的技术栈热度、薪资分布及行业需求趋势。

## 📌 功能特性

- **多源智能爬取**：支持面向 JobsDB (GraphQL/REST API) 的高效请求爬取，并内置 Playwright 自动化引擎以应对复杂的动态渲染与反爬墙。
- **混合文本清洗**：针对香港求职市场特有的中英夹杂（Code-switching）文本，提供精准的噪声去除与文本规范化。
- **双引擎技能提取**：
  - *Rule-Based 引擎*：基于高性能正则边界匹配，秒级处理海量数据。
  - *LLM-Based 引擎*：集成大模型 API（如 DeepSeek / OpenAI），智能理解上下文，抽取隐含的架构能力与软实力。
- **多维度可视化**：一键生成技术热度榜、薪资-技术关联箱线图以及热门工作区域分布图。

## 🛠️ 架构设计

```
[数据源: JobsDB / Indeed] ──> [数据采集层: Requests/Playwright]
                                       │
                                       ▼
[结构化存储: MongoDB/CSV]  ──> [数据清洗层: Pandas/Regex]
                                       │
                                       ▼
[可视化看板: Streamlit/Matplotlib] <── [分析挖掘层: Dual-Engine NLP/LLM]

```

## 📦 快速开始

### 1. 环境依赖
确保你的系统已安装 Python 3.9 或以上版本。

```bash
# 克隆仓库
git clone https://github.com/yourusername/HK-JobMarket-Analyzer.git
cd HK-JobMarket-Analyzer

# 安装依赖
pip install -r requirements.txt

# 安装 Playwright 浏览器内核（如需启用自动化爬虫机制）
playwright install chromium

```

### 2. 配置环境
在项目根目录下创建一个 `.env` 文件，并配置相关密钥与代理（可选）：

```代码段
# 代理配置（建议使用香港本地或海外住宅代理）
PROXY_HTTP=http://your_hk_proxy_ip:port
PROXY_HTTPS=http://your_hk_proxy_ip:port

# 大模型 API 配置（若使用 LLM 提取引擎）
LLM_API_KEY=your_deepseek_or_openai_api_key
LLM_BASE_URL=https://api.deepseek.com/v1

```

### 3. 运行系统
系统采用模块化设计，你可以分步运行或执行一键工作流：

#### 步骤一：数据采集

```bash
python src/crawler.py --keyword "Full Stack" --pages 5

```

#### 步骤二：数据分析与技术提取

```bash
# 使用正则词表引擎提取
python src/analyzer.py --mode rule

# 或使用大模型智能引擎提取
python src/analyzer.py --mode llm

```

#### 步骤三：启动可视化看板

```bash
streamlit run src/app.py

```

## 📊 数据产出示例
运行成功后，系统将在 `output/` 目录下生成可视化图表，或直接在 Streamlit 网页端呈现：

- **Top In-Demand Skills**: 直观展示香港当前前端（React/Vue）、后端（Node.js/Spring Boot）及云服务（AWS/Azure）的市占率。
- **Salary Insights**: 自动将 `HK$45,000 - HK$60,000 /month` 等非结构化薪资转化为数字区间，并关联技术栈展示高薪技能组合。

## 📁 项目目录结构

```plaintext
├── config/                  # 配置文件（技术栈词表、爬虫请求头等）
│   └── tech_dict.json
├── src/                     # 源代码目录
│   ├── crawler.py           # 爬虫核心模块
│   ├── cleaner.py           # 数据清洗与薪资标准化模块
│   ├── analyzer.py          # 正则与LLM文本挖掘模块
│   └── app.py               # Streamlit 可视化界面
├── output/                  # 导出数据与图表存放处
├── requirements.txt         # 项目依赖列表
└── README.md                # 项目说明文档

```

## ⚖️ 法律与合规声明 (Disclaimer)

1. 本系统仅用于**学术研究与个人求职数据分析**，请勿将相关数据用于任何商业牟利行为。
2. 数据采集频率已默认接入随机延迟（2.5s - 5.0s），请勿恶意加大并发，严格遵守目标网站的 `robots.txt` 协议，共同维护良好的网络生态。
3. 因不当使用本工具造成的目标网站服务过载或法律纠纷，由使用者自行承担。

## 🤝 贡献与反馈
欢迎提交 Issue 或 Pull Request！如果你有任何改进意见（例如新增香港本地招聘网站源或优化 Prompt 提示词），请随时联系。

- Email: your.email@example.com
- Maintainer: @yourusername

---

*Exported from [Voyager](https://github.com/Nagi-ovo/gemini-voyager)*  
*Generated on June 1, 2026 at 12:31 PM*